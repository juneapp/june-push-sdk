{
  "name": "june-push-sdks",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo für die June Push SDKs (Web, React Native) + gemeinsame Types.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared && npm run build -w packages/web && npm run build -w packages/react-native",
    "prepare": "npm run build",
    "clean": "npm run clean --workspaces --if-present",
    "publish-all": "npm publish --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.14.0"
  }
}
